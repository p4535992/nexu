name: Reverify two-module build

on:
  push:
    branches:
      - spring-boot-modernization-2
    paths:
      - '.github/workflows/reverify-two-module-build.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: spring-boot-modernization-2
          fetch-depth: 1
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: Compile and test
        id: build
        shell: bash
        run: |
          set +e
          mvn --batch-mode --errors clean package -Dmaven.test.skip=true > two-module-build.log 2>&1
          build_status=$?
          test_status=99
          if [ "$build_status" -eq 0 ]; then
            mvn --batch-mode --errors -pl nexu-app -am test > two-module-test.log 2>&1
            test_status=$?
          else
            echo 'Tests not run because compilation failed.' > two-module-test.log
          fi
          {
            echo "build_status=$build_status"
            echo "test_status=$test_status"
            echo
            echo '=== build log tail ==='
            tail -n 320 two-module-build.log
            echo
            echo '=== test log tail ==='
            tail -n 320 two-module-test.log
          } > docs/ci/two-module-verification.txt
          echo "build_status=$build_status" >> "$GITHUB_OUTPUT"
          echo "test_status=$test_status" >> "$GITHUB_OUTPUT"
      - name: Persist verification
        if: always()
        run: |
          rm .github/workflows/reverify-two-module-build.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A docs/ci/two-module-verification.txt .github/workflows/reverify-two-module-build.yml
          git commit -m 'Record consolidated build verification'
          git push origin HEAD:spring-boot-modernization-2
      - name: Fail when verification failed
        if: always()
        run: |
          test '${{ steps.build.outputs.build_status }}' = '0'
          test '${{ steps.build.outputs.test_status }}' = '0'

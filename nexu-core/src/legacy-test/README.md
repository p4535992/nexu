# Legacy tests

These tests are preserved outside Maven default test sources because they depend on removed DSS 5 APIs, Log4j 1.x fixtures or retired server implementations. Migrate each test to DSS 6.4 and JUnit 5 before returning it to `src/test`.
